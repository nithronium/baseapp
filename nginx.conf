server {
    listen       3000;
    server_name  localhost;

    rewrite ^/(.*)/$ /$1 permanent;
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html?/$request_uri;
    }
}
