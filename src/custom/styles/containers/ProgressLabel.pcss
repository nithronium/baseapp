.pg-trading-header-progress-label {
  &-additional {
    color: var(--placeholder-text);
  }

  &-progress {
    &-positive, &-negative {
      font-family: var(--font-family);
      font-style: normal;
      font-weight: bold;
      font-size: 14px;
      line-height: 19px;
    }
  }
}
