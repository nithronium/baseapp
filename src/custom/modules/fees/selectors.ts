// import { RootState } from '../../../modules/index';
// import { FeesState } from './reducer';
// import { Fees } from './types';

// const selectFeesState = (state: RootState): FeesState => state.public.fees;

// export const selectFees = (state: RootState): Fees[] => selectFeesState(state).list;

// export const selectFeessLoading = (state: RootState): boolean | undefined => selectFeesState(state).loading;
