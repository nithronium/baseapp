const grid = {
    width: '900px',
};

const actionCode = {
    textOverflow: 'ellipsis',
    maxWidth: '200px',
    display: 'inline-block',
    overflow: 'hidden',
};

const icon = {
    cursor: 'pointer',
    width: '20px',
    height: '20px',
};

const iconActive = {
    ...icon,
    fill: '#00C620',
};

export const styles = {
    actionCode,
    grid,
    icon,
    iconActive,
};
