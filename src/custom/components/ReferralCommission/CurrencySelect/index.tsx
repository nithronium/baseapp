/* tslint:disable */
import * as React from 'react';

export const CurrencySelect = ({currencyId}) => {

    return (
        <a href="#!" className="round-button default arrow">
            {currencyId.toLocaleUpperCase()}
        </a>
    )

}


